{
  "name": "@ghostry/extern-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "pkg/*"
  ],
  "overrides": {
    "@ghostry/extern": "workspace:*"
  },
  "scripts": {
    "build": "bun --cwd=pkg/extern run build && bun --cwd=pkg/extern-extension-fabricator-v0 run build",
    "check": "bun --cwd=pkg/extern run check && bun --cwd=pkg/extern-extension-fabricator-v0 run check",
    "test": "bun --cwd=pkg/extern run test && bun --cwd=pkg/extern-extension-fabricator-v0 run test",
    "format": "bunx --no-install prettier --write ."
  },
  "devDependencies": {
    "@rslib/core": "^0.21.5",
    "@types/bun": "^1.3.14",
    "jsr": "^0.14.3",
    "npm": "^11.15.0",
    "prettier": "^3.8.3",
    "sury": "^11.0.0-alpha.5",
    "typescript": "^6.0.3"
  }
}
