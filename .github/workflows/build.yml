name: Build

on:
  workflow_call:
    outputs:
      run-id:
        value: ${{ github.run_id }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Upload `@ghostry/extern`
        uses: actions/upload-artifact@v7
        with:
          name: pkg-extern
          path: pkg/extern/dist
          if-no-files-found: error

      - name: Upload `@ghostry/extern-extension-fabricator-v0`
        uses: actions/upload-artifact@v7
        with:
          name: pkg-extern-extension-fabricator-v0
          path: pkg/extern-extension-fabricator-v0/dist
          if-no-files-found: error
